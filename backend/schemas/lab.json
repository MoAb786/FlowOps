{
  "type": "object",
  "properties": {
    "event_type": {
      "type": ["string", "null"],
      "enum": ["issue component", "return component", null]
    },

    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": ["string", "null"]
          },
          "quantity": {
            "type": ["integer", "null"],
            "minimum": 1
          }
        }
      }
    },

    "needs_human_clarification": {
      "type": "boolean"
    }
  },

  "required": [
    "event_type",
    "items",
    "needs_human_clarification"
  ]
}